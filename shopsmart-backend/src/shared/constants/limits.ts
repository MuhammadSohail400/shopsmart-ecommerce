/** BR-008: max order quantity per SKU per order, admin-configurable in future (Settings module). */
export const MAX_ORDER_QUANTITY_PER_SKU = 10;

/** BR-009: minimum order value, 0 = disabled at launch. */
export const MIN_ORDER_VALUE = 0;

/** Guest cart TTL in seconds (30 days, per SDD Section 11.3). */
export const GUEST_CART_TTL_SECONDS = 60 * 60 * 24 * 30;
