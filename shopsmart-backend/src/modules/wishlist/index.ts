/**
 * Wishlist Module — public interface.
 * Responsibility: saved-for-later products for registered users.
 * Dependencies: products (existence check), cart (move-to-cart handoff).
 */
export { wishlistRoutes } from './wishlist.routes';
